// Minimal umbrella header exposing just the mGBA core API ArchiveForge's
// Swift bridge (MGBACore.swift) actually calls — not mGBA's entire surface
// (debugger, scripting, etc.), which this app has no use for.
#ifndef ARCHIVEFORGE_CMGBA_SHIM_H
#define ARCHIVEFORGE_CMGBA_SHIM_H

#include <mgba/core/core.h>
#include <mgba/gba/core.h>
#include <mgba-util/vfs.h>

#endif
